package ocajp8.chapter2.questions.q10;

public class Question10 {

	/*
	 * What is the output of the following code?
	 * 
	 * 3: byte a = 40, b = 50;
	 * 4: byte sum = (byte) a + b;
	 * 5: System.out.println(sum);
	 * 
	 * A. 40
	 * B. 50
	 * C. 90
	 * D. The code will not compile because of line 4. (ok) -> byte + byte -> int
	 * E. An undefined value.
	 */
	
	public static void main(String[] args) {
//		byte a = 40, b = 50;
//		byte sum = (byte) a + b;
//		System.out.println(sum);
	}
}
